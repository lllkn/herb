/**
 * 游戏配置模块 - GameConfig
 * 定义游戏的核心常量和配置参数
 */

const GameConfig = {
    // 画布尺寸
    GAME_WIDTH: 1280,
    GAME_HEIGHT: 720,

    // 世界尺寸（可滚动区域）
    WORLD_WIDTH: 1600,
    WORLD_HEIGHT: 1200,

    // 玩家配置
    player: {
        speed: 160,
        size: 40,
        radius: 20,
        collectDistance: 50,  // 采集距离
        diagonalFactor: 0.707 // 对角线移动修正系数
    },

    // 草药颜色映射
    herbColors: [
        0x4a7c28,  // 绿色 - 普通
        0x8b7355,  // 棕色 - 普通
        0xff6b6b,  // 红色 - 稀有
        0x8b0000,  // 深红 - 稀有
        0xf0e68c   // 黄色 - 优良
    ],

    // 稀有度颜色配置（用于UI显示）
    rarityColors: {
        common: '#88AA77',     // 普通 - 绿色
        uncommon: '#5599CC',   // 优良 - 蓝色
        rare: '#AA66BB',       // 稀有 - 紫色
        epic: '#CC7744',       // 史诗 - 橙色
        legendary: '#FFAA22'   // 传说 - 金色
    },

    // 时辰系统配置
    timeSystem: {
        periods: ['寅时', '卯时', '辰时', '巳时', '午时', '未时', '申时', '酉时', '戌时', '亥时', '子时', '丑时'],
        tickInterval: 1000,   // 每秒一个tick
        ticksPerPeriod: 12,   // 每12个tick切换一次时辰（12秒）
        ticksPerDay: 144,     // 一天144个tick（12时辰×12tick），即144秒=一天
        defaultTime: '寅时',
        defaultDate: '九月十七',
        defaultSeason: '寒露'
    },

    // 节气系统配置（农历月份 → 节气映射）
    solarTerms: {
        // 格式: "月份": { 节气名: 所在旬(上/中/下) }
        '正月':  ['立春', '雨水'],
        '二月':  ['惊蛰', '春分'],
        '三月':  ['清明', '谷雨'],
        '四月':  ['立夏', '小满'],
        '五月':  ['芒种', '夏至'],
        '六月':  ['小暑', '大暑'],
        '七月':  ['立秋', '处暑'],
        '八月':  ['白露', '秋分'],
        '九月':  ['寒露', '霜降'],
        '十月':  ['立冬', '小雪'],
        '十一月': ['大雪', '冬至'],
        '十二月': ['小寒', '大寒']
    },

    // 月份顺序（用于日期推进）
    monthOrder: ['正月','二月','三月','四月','五月','六月','七月','八月','九月','十月','十一月','十二月'],

    // 天气系统
    weathers: ['晴', '多云', '阴', '小雨', '雾'],

    // 草药初始位置配置
    herbPositions: [
        { x: 300, y: 300, type: 0 },   // 甘草
        { x: 500, y: 400, type: 1 },   // 黄芪
        { x: 700, y: 500, type: 2 },   // 当归
        { x: 900, y: 300, type: 3 },   // 枸杞
        { x: 1100, y: 600, type: 4 },  // 金银花
        { x: 400, y: 700, type: 5 },   // 菊花
        { x: 1000, y: 350, type: 6 }   // 陈皮
    ],

    // 障碍物配置
    obstacles: [
        { x: 400, y: 300, w: 60, h: 60 },
        { x: 800, y: 500, w: 80, h: 40 },
        { x: 600, y: 700, w: 40, h: 80 }
    ],

    // 摄像机跟随配置
    camera: {
        followLerpX: 0.08,
        followLerpY: 0.08
    }
};

// 导出供其他模块使用（全局变量模式）
window.GameConfig = GameConfig;
